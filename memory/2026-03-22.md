# 2026-03-22 — Системная настройка

## Выполнено

### 1. Skills исправлены
- Пути: `/home/pico/` → `/home/openclaw/`
- Владелец: `openclaw:openclaw`
- Удалён: `gipsy-digest`
- Осталось: crypto, pdf-to-md, playfetch, workspace-startup

### 2. Exec approval отключен
**Конфиг:**
```json
"tools": {
  "exec": {"ask": "off", "security": "full"},
  "elevated": {
    "enabled": true,
    "allowFrom": {"telegram": ["34793946"]}
  }
}
```
**Результат:** exec-команды работают без approval-ошибок

### 3. SQLite база для индексации файлов
**Создано:**
- `/home/openclaw/.openclaw/workspace/data/file_index.db`
- Таблицы: files, file_content, tags, file_tags, file_search (FTS5)
- Скрипты: init_db.py, index_files.py, search.py
- Проиндексировано: 24 файла

**Использование:**
```bash
python3 /home/openclaw/.openclaw/workspace/skills/file-index/search.py "запрос"
```

### 4. Env переменной
```bash
export NODE_COMPILE_CACHE=/var/tmp/openclaw-compile-cache
export OPENCLAW_NO_RESPAWN=1
```

## Конфиг openclaw.json

- memorySearch: ollama + qwen3-embedding:0.6b ✅
- plugins: telegram, openclaw-web-search, lossless-claw ✅
- elevated: включено для 34793946 ✅
- exec.ask: off ✅

## Заметки на будущее

- Не трогать конфиг без команды
- Exec теперь работает без approval
- File-index готов для поиска по workspace
- Playwright установлен, browser tool требует системный браузер

## ✅ Финальный статус (22:54)

**Все права исправлены:**
- workspace/*.md — openclaw:openclaw ✅
- workspace/skills/ — openclaw:openclaw ✅
- workspace/data/file_index.db — openclaw:openclaw ✅

**Конфиг:**
- tools.exec.ask = "off" ✅
- tools.elevated.enabled = true ✅
- allowFrom.telegram = 34793946 ✅
- memorySearch.provider = ollama ✅

**Skills (5):**
- crypto — курсы криптовалют
- file-index — SQLite + FTS5 поиск
- pdf-to-md — конвертация PDF
- playfetch — Playwright парсинг
- workspace-startup — файлы при старте

**Env:**
- NODE_COMPILE_CACHE=/var/tmp/openclaw-compile-cache
- OPENCLAW_NO_RESPAWN=1
